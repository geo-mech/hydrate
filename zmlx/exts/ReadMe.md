## 关于exts模块

对于exts模块，主要是提供dll扩展，并实现最为基本的接口函数/类。在exts中，不定义任何复杂的操作。所有的函数，都应该是清晰的、明确的。
比如，对于Seepage类，在迭代的时候需要dt，在相应的函数中，就应该显式地给出dt，而不是让Seepage类在dll内部去读取dt的属性。

尽可能地保证操作的细节在Python层面是透明的、可见的，这对于整个模块的可维护性是很重要的。“仅仅定义最基本接口”，即，一个dll的导出
函数，对应一个Python函数，这是整个exts模块的宗旨。后续，会逐步将不符合此宗旨的功能从exts中移除。

