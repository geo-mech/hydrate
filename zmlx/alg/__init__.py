"""
定义常用的一些算法。这些算法大部分和zml无关。目前，这个包中的内容有些混乱，会逐渐在后续的版本中
去进行整理。另外，后续会逐步避免模块/包的名字与函数/类重名。

主要包括如下模块：

alpha:
    临时定义处于测试阶段的函数。当稳定之后，会转移到其它位置。

frac:
    裂缝相关（主要用于测试）

fsys:
    文件系统相关的工具。

image:
    图像相关的工具。

interp:
    插值相关的函数.

marked_text:
    标记文本相关。

multi_proc:
    多进程计算相关的函数.

multi_thread:
    多线程计算相关的函数.

np:
    numpy相关的函数。

os:
    操作系统相关的函数。

sys:
    系统相关的函数。

utils:
    工具类函数。是一些尚未分类的函数。
"""
